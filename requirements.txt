# Legacy pip install — prefer: uv sync
# Regenerate from lock: uv export --no-dev --no-emit-project -o requirements.txt
sentence-transformers>=2.2.0
lancedb>=0.6.0
numpy>=1.24.0
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pyarrow>=14.0.0
